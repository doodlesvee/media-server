// Schema tables land here starting Phase 2 (media_items, tags, collections, etc).
// Intentionally empty for the Phase 1 scaffolding pass — this file just needs to
// exist so drizzle-kit has a valid (if currently empty) schema to point at.
export {};
